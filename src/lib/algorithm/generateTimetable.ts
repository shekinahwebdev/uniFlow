import { Course, TimetableEntry } from "../../types";

export function generateTimetable(courses: Course[]): TimetableEntry[] {
  // Placeholder: simple assignment sequentially
  return [];
}

export default generateTimetable;
